var assert = require('assert');

describe('userDao', function() {
  it('should be able to insert new users into db', function() {
  });

  it('should be able to update users in db', function() {
  });

  it('should be able to delete users from db', function() {
  });

  it('should be able to find all users from db', function() {
  });
});
